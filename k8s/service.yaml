apiVersion: v1
kind: Service
metadata:
  name: agent-relay-service
  labels:
    app: agent-relay
spec:
  selector:
    app: agent-relay
  ports:
    - name: http
      port: 8000
      targetPort: http
  type: ClusterIP